import 'package:flutter_test/flutter_test.dart';

import 'package:notdryjanuary/main.dart';

void main() {
  test('App root widget can be constructed', () {
    expect(const NotDryJanuaryApp(), isNotNull);
  });
}
